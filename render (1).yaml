services:
  - type: web
    name: maths-nerds-multiplayer
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    env:
      - key: NODE_ENV
        value: production
    domains:
      - math-battle.onrender.com
